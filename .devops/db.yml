name: Dummy DB

pool:
  vmImage: ubuntu-latest

trigger:
  branches:
    include:
      - master
  paths:
    include:
      - db
pr: none

stages:
  - stage: Hello_World
    jobs:
      - job: Foo
        steps:
          - script: echo Hello World!